# MI as RP

- Amount of stoch in the system (both this and the gamma influence the number of learnable bits, so really it’s information), this governs observed distinctions in replan, but for us it’s latent distinctions
    - not entirely - it governs state distinctions, and they in turn govern the mapping between the states and reward/value. so the latent there is really the r values, and how many distinctions I will be making in those

- what is the relationship between information maximisation and planning in the space of representations?
- the BA is a konvex optimisation iteration to find the maximum, which corresponds to infinite computational capacity
- planning is improving on value step by step, which I believe also ends up being convex, and executing it fully corresponds to infinite computation as well
    - wait I think it’s only convex if you already know the model or something similar - or maybe not?
        - the root sampling inside loop should be still convex as that is conditioned on one model
    - in a sense, planning isolates the external cost, that of the information, by computing everything that is possible to compute using the data we already have
    - the result of the planning maximises value-that very much isn’t information, the value of information is just one component in it
    - but when the entire task is learning, then is it true that the value of information equals value? - sure sounds like it
        - and then this would correspond to a VAE-IB dichotomy, where if the IB rhs variable is the same as the lhs one, you get back a VAE
    - there is also the path dependency thing that the planning addresses, and the MI doesn’t, because it’s easier that way, but maybe the daisy chain could get it back in some way?
    - so is the MI maximisation a case when an intractable tree-shaped computation can be reduced to a merely absurdly costly polynomial one, similarly to value iteration
    - so MI is akin to the finite horizon planning, and then the Jeffreys would be the asymptotic one? but with what choice for a discount factor?
        - or maybe it isn’t exponential discounting?
        - information is not perfectly analogous with value in the sense that it cannot be accumulated indefinitely, there is just so much information in any given source
        - does the learning task remove the temporal contingencies? I think it does - so does the contextual bandit though
    - the nature of time in these two cases is also different. in an MDP, contingencies dictate how much of the target you achieve, with the MI, they are just recalibrate what you want to answer
    - there is the problem of “evaluation” as well. when we say there is an N, and then again a different one, we say between the two there was some sort of an evaluation, like calculating the KL between some ground truth and the estimated posterior, and getting a reward based on that.
        - is this the evaluation we expect typically? or at least some sort of sample-based approximation to it, like a betting scenario or something
        - if so, this is again a simplified class of MDPs, where there is a series of passive states and then an active one with no way to learn, and the agent knows how many passive ones are expected
    - there was this point about fully bayesian updates not producing coarse-graining even in finite time. what’s up with that?
        - I used non-counterfactual beliefs, what was that? - something like not updating beliefs for states I’m not in or something like that - that being representational states! so I’m not maintaining beliefs for all possible representations in parallel - ok that’s not a very strong constraint
    - there was the non-markovian threshold satisficing reward functions part, which made coarse-garining optimal
        - satisficing and limited information probably goes hand in hand - the threshold basically *is* limited information in the sense that all I know I want is to be there, with no further distinctions
    - the amount of stochasticity in the system! this seems important for the MI and was in RP as well
    - inference-as-planning: [https://proceedings.mlr.press/v108/buesing20a/buesing20a.pdf](https://proceedings.mlr.press/v108/buesing20a/buesing20a.pdf)